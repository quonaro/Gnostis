run:
  desc: Start the Gnostis server
  native: true

install:
  desc: Install gnostis as a systemd user service
  native: true

config:
  desc: Show interpolated config with secrets masked
  native: true

decrypt-cascade:
  desc: Decrypt Windsurf Cascade .pb files and export them as Markdown
  native: true
  vars:
  - "OUTPUT_DIR="
